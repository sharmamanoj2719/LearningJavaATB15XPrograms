package ex_03_Java_Variables;

public class Lab012_InterviewQ {
    public static void main(String[] args) {
        int Enum = 10;
        System.out.println(Enum);

        // Enum is reserve key word so , we can't use as a variable
        // But we can use Enum

    }
}
